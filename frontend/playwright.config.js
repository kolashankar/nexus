import { defineConfig, devices } from '@playwright/test';

export default defineConfig({
  testDir,
  fullyParallel,
  forbidOnly,
  retries,
  workers,
  reporter,
  
  use,
    trace,
    screenshot,
    video,
  },

  projects,
      use,
    },
    {
      name,
      use,
    },
    {
      name,
      use,
    },
    /* Mobile viewports */
    {
      name,
      use,
    },
    {
      name,
      use,
    },
  ],

  webServer,
    url,
    reuseExistingServer,
    timeout,
  },
});
