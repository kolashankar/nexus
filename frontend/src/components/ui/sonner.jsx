import { Toaster as Sonner } from 'sonner';

const Toaster = (props) => {
  return (
    <Sonner
      className="toaster group"
      toastOptions={{
        classNames
          toast
            'group toast group-[.toaster],
          description,
          actionButton,
          cancelButton,
        },
      }}
      {...props}
    />
  );
};

export { Toaster };
